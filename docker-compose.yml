version: '3.3'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: hubspot-app
    ports:
      - "8080:8080"
    networks:
      - hubspot-net

networks:
  hubspot-net:
    driver: bridge
